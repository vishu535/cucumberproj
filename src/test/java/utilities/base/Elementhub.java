package utilities.base;

import org.openqa.selenium.By;

public class Elementhub {

	public By signinlink = By.cssSelector("#header-utils li:nth-child(3) a");
	/*
	 * public void clickSignin(String username, String pwd) {
	 * selcommands.clickElement(signinlink, "signlink");
	 * selcommands.clickElement(signinlink, "signlink");
	 * selcommands.clickElement(signinlink, "signlink"); } public void
	 * clickSignin(String username, String pwd) {
	 * selcommands.clickElement(signinlink, "signlink");
	 * selcommands.clickElement(signinlink, "signlink");
	 * selcommands.clickElement(signinlink, "signlink"); } public void
	 * clickSignin(String username, String pwd) {
	 * selcommands.clickElement(signinlink, "signlink");
	 * selcommands.clickElement(signinlink, "signlink");
	 * selcommands.clickElement(signinlink, "signlink"); }
	 */
	
}
